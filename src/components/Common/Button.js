import React from "react";

const Button = ({ title }) => {
  return (
    <div class="button" id="button-5">
      <div id="translate"></div>
      <p>{title}</p>
    </div>
  );
};

export default Button;
